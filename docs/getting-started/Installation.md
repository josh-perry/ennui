Copy the `ennui` folder into your project. I'm going to assume that it's placed in a `lib` folder but it can go wherever you like, just update the require paths where relevant:
```
.
├── main.lua
├── conf.lua
└── lib/
    └── ennui
```

